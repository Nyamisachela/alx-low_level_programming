#include "main.h"

/**
 * main - tests positive_or_negative() gives correct output when given 0.
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
