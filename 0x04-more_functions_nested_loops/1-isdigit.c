#include "main.h"

/**
 * _isdigit - a function that checks for a digit (0 through 9)
 * @c: parameter
 * Return:return always 1 if c is a digit otherwise return 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
